pipeline {
    agent any
    stages {
        stage('COMPILE') {
            steps {
                script {
                    echo "compile the code"
                }
            }
        }
        stage('TEST') {
            steps {
                script {
                    echo "compile the code"
                }
            }
        }
        stage('PACKAGE') {
            steps {
                script {
                    echo "compile the code"
                }
            }
        } 
        
    }
}